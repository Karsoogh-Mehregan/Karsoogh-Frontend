# Local dev: cp .env.example .env
#
# Production Docker (gitignored): .env.prod — CI writes it from PROD_* GitHub Variables
# Develop Docker: pass -e VITE_API_BASE_URL=... at docker run (see Dockerfile.dev)
APP_ENV=development
VITE_API_BASE_URL=http://localhost:8000